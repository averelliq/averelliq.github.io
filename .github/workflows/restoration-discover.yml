name: Restoration Shorts - Example-style discovery

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/restoration-discover.yml'
      - 'restoration-shorts-bot/**'
  workflow_dispatch:
  schedule:
    - cron: '20 8 * * *'

permissions:
  contents: read

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    defaults:
      run:
        working-directory: restoration-shorts-bot
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Test example-style selection rules
        run: python -m unittest discover -s tests -v
      - name: Search rusty-object full-restoration metadata
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: |
          if [ -z "$PEXELS_API_KEY" ]; then
            echo 'Existing PEXELS_API_KEY is unavailable in this GitHub repository.' >&2
            exit 1
          fi
          python style_bot.py discover
      - name: Save clickable previews and honest review report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: restoration-example-style-candidates
          path: |
            restoration-shorts-bot/output/candidates.json
            restoration-shorts-bot/output/review_report.json
            restoration-shorts-bot/output/review_gallery.html
          if-no-files-found: warn
          retention-days: 7
